import tw, { styled } from 'twin.macro';

export const ContainerStyles = styled.div`
	${tw`h-screen`}
	background: linear-gradient(
		291.34deg,
		rgba(234, 75, 78, 0.12) 0%,
		rgba(242, 110, 97, 0.0948) 100%
	);
`;
